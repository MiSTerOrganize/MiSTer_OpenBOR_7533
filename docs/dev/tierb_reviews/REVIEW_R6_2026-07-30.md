# Tier-B Phase 1 — ROUND 6 review ledger (2026-07-30)

Two reviewers over `d6f855e`: engine-fact and numbers. Both recomputed from primary
sources (pristine v7533 whole-tree, `apply_patches.py`, the 450-PAK library, the raw
scan TSVs) rather than from the document's own prose.

**Criticals found in each round's fixes: 8 → 6 → 10 → 6 → 5 → 3.** Declining, but the
failure mode did not change, and round 6's two headline findings are the same shape as
round 3's: *round 5 narrated corrections it did not make, and one correction it did make
was itself wrong.*

The heavy numeric spine re-derived **exactly** — all 20 band rows with zero overlaps, the
entire bandwidth model, every tooling-defect figure, the M10K and timing budgets, the
px/clk chain, and every RTL line citation. The defects are in the prose that surrounds it.

---

## A. Round 5's corrections, re-checked

| R5 claim | verdict |
|---|---|
| census 283 / 97 (USMB is 320x240) | ✅ recomputed from `pak_videoscan_2026-07-29.tsv` |
| variant 4 = 98, variant 5 = 295 | ✅ |
| 341 right / 109 wrong | ✅ |
| traces 274/283 and 90/97 | ✅ correct — **and never written into the document** (E1) |
| only 3 of 7 exit-139 are SIGSEGV | ✅ names match `FINDINGS.md` exactly |
| D1: delete the `BGT_BACKGROUND` §9.7 row | ❌ **wrong — the row is live** (D-1) |

## B. CRITICAL

| # | finding |
|---|---|
| **D-1** | 🛑 **R5's D1 deleted a row that exists in the ship build, and it is measurable.** The premise ("every layer screen is `PIXEL_x8`") is right but does not reach this path: `load_layer` is explicitly **not** called for a `background` line (`openbor.c:20481-20483`); that layer's screen is *aliased* to the global `background` (`:21292-21296`), which ship-build Step 23 converts to **PIXEL_16**. It is then drawn from `layersref` with a **non-NULL** drawmethod (`:45151`), so `_putscreen` takes its `else` (`screen.c:509-515`), reads `transbg`, and lands on `blendscreen16` keyed on RGB565 `0x0000`. `transbg` there is PAK-authored (`:20435`). **Measured over the 450-PAK library: 19,086 `background` lines, 460 with `transbg != 0`, across 31 PAKs (6.9%).** Restore the row as a second `(src_fmt, mode) = (0,1)` — the encoding needs no change — and delete "the only source where that key is armed" from row 4. The key is in the **predictable** bucket. |
| **E1** | 🛑 **`:1795` still carries the pre-correction trace coverage** (`91/98`, `273/282`; correct is `90/97`, `274/283`). R5's own ledger lists this as applied; `git show` confirms neither `0684593` nor `f9a1fbc` touched it. `:1810-1812` sits directly beneath it and *correctly states* that these are the pre-correction numbers — the document documents its own error instead of fixing it. Round 3's exact failure mode, reproduced at round 5. |
| **E5** | 🛑 **The implicit coverage-plane address cannot work for a multi-band `LINEAR`.** `src_addr` is the first row pre-seeked and `n_rows` is rows *in this band* (`:230-231`), and §7.3 emits one list per band — so `src_addr + n_rows*src_stride` lands inside the next band's pixel rows, not at the coverage plane. The doc's own sizing example is the counter-example: a 960x480 fallback spans **15** of He-Man's 33-line bands, and `+57,600 B` is a whole-bitmap plane only one command can address. The arithmetic (921,600 and 57,600) is right; the addressing scheme under it is not. |

## C. MAJOR — propagation

| # | finding |
|---|---|
| **D-3 / E6** | Both reviewers independently: **§9.11.5 never received D10's 11th path.** §9.10 `:1051` adds `openbor_drawspriteq` and routes the reader to 9.11.5; 9.11.5 still opens *"The **three** that cannot are:"* over a 3-row table without it, `:988` and `:1374` still say three, and §9.10's *"no (9.11.5)"* is now a dangling cross-reference. For a section headed *"THE CANONICAL LIST — every other enumeration in this document must match it"*, four enumerations don't. |
| **D-2** | §7.2's `LINEAR` prose (`:245-249`) contradicts the rebuilt §9.7 three ways: "fullscreen background is fully opaque" (false for D-1's 460 lines); "a plainly-blitted parallax layer keys on `0x0000`" (row 3 keys on **palette index 0**); "three-case table" (five rows since `0684593`). |
| **E8 / D-3** | `:1375` vs `:1377`, one sentence apart: *"These **three** paths"* … *"omitted all **four**"* … then *"(Note there is no 'fade in' path…)"* refuting the "fade in/out" it just listed. |

## D. Numbers — arithmetic and status

| # | finding |
|---|---|
| E2 | `:604` "the **110** are the **99** variant-4 PAKs plus 11" — 98+11 = 109, and 341+110 = 451 ≠ 450. `:602` two lines up already says 109. |
| E3 | `:619` "the **282** PAKs" where `:628` and `:721` say 283. |
| E4 | `:751` build order says variant 4 carries **99**; the table at `:627` says 98. |
| E7 | `:1940` asserts 20.0/29.0 ms "are measured today"; `:19-22` retires that pair as superseded by the 41-sample anatomy (21–29 ms / 34–48 fps). 20.0 ms = 50 fps, outside the current measured range. |
| E9 | "~360" is not the sum of the terms §14.4.5 names: He-Man applies all three omitted port terms (122.57 ✅), Ninja applies only two (362.72 vs the quoted ~360 — the command term is dropped). Also 1 dp vs 2 sf. |
| E10 | `:1898`'s code block still says `measured overdraw: max 38.56x` six lines after the prose retracts the word "measured". |

## E. Citations (cosmetic, all verified off-by-small)

`:280` → `screen16.c:83-91` (not 79-87); `:550` → `apply_patches.py:5005`; `:539` →
`blend_half16` is 583-591, 593-596 is `blend_tint16`; `:1181` ST_CHECK_CTRL is two hops
from `ST_IDLE` (`:495`→`:505`→`:509`), latch-once conclusion unaffected; `:1035` says ten
paths over an 11-row table; `:1133` cites `load_cached_background` as `load_background`
(benign — `openbor.c:3951` delegates when `CACHE_BACKGROUNDS` is undefined, which the
MiSTer build leaves undefined); `:800-802` and `:813-816` are the same paragraph twice,
and both cite `:20505`/`:20745` as "PAK-authored" when they are engine defaults.
`D-5`: `:556`'s "the only other writers are dead" is the wrong *reason* for a right
conclusion — `videocommon.c:45/:65` are live but take `s_videomodes` **by value**, which is
a stronger guarantee than deadness and won't mislead a future grep.

## F. Measured — this closes registered item 7f

Engine-exact evaluation of `openbor.c:4127` (with `alphamask` state tracked as the engine
tracks it) over all 450 PAKs:

| | lines | |
|---|---:|---|
| `bglayer`/`layer`/`fglayer` total | 20,264 | |
| → `loadsprite2` (sprite, **not** `LINEAR`) | 14,870 | 73.4% |
| → `loadscreen` (**the `LINEAR` population**) | 5,394 | 26.6%, 121 PAKs |
| ├ `transbg == 0` → row 2 | 729 | |
| └ `transbg != 0` | 4,665 | 93 PAKs |
| ├ `amplitude == 0` → **row 3, keyed `putscreenx8p16`** | **470** | **48 PAKs** |
| └ `amplitude != 0` → water/plane → row 5 | 4,195 | |

Consistency gate: `transbg != 0 && watermode == 0` in the screen-loaded set = 0, as the
engine condition requires. **R5's D2 retraction is confirmed and now quantified.**
Corollary the document does not state: **77.8% of screen-loaded layers are
water-rasterised (row 5), not rows 2/3.** Static upper bound — runtime frequency needs a
device trace.

**Register item 12 is also answerable from data already in the repo:** `pak_blendscan`
gives **67 / 450** PAKs declaring `tint` and 1 declaring `channel`; He-Man's runtime line
is `tint=2734` of 12,779 blits = **21.4%**. §9.5 says the tint population "must be
measured, not assumed rare" — it is not rare.

---

## Still open going into round 7

1. 🛑 **433 MB/s ceiling** (item 0) — the f2h probe is **being built and run now**
   (`fpga/rtl/tierb_bw_probe.sv` + `tierb_ddr_arb.sv`, `tools/harness/read_bw_probe.sh`).
   This is the one item that cannot be closed by more reading.
2. 🛑 **Compositing rate** — fetch engine ~1 px/clk is the binding constraint. Note the
   unit caveat: 5.44 Mpx is unclipped bounding-box area while the ~1 px/clk bound counts
   RLE-visible pixels; the two are not in the same units and the gap is unquantified.
3. **E5's coverage-plane addressing** — needs an explicit `cover_addr` per command, or a
   whole-bitmap plane addressed once with a band-relative row offset.
4. **D-1's row restored**, and its runtime weight (460 authored lines is a static bound).
5. **BUF2 propagation** — 2-bit target indices, a `BUF2_ADDR`, a §6 row, and the address
   space to hold it; declared mandatory in round 5 and propagated nowhere.
6. The E2/E3/E4/E7/E9/E10 arithmetic and status fixes, and the §9.11.5 enumeration.
7. MB-vs-MiB conventions; §8.1's "impossible" labels vs the budget test (Gunman's 30,000 px
   **fits** 32,768 and should be excluded from "8 PAKs broken").

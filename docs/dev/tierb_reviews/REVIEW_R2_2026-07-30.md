# Tier-B Phase 1 — ROUND 2 review ledger (2026-07-30)

Four independent reviewers over the post-round-1 document. Round 1 is preserved in
`REVIEW_2026-07-30.md`. This file is the finding ledger + what was done about each, so
round 3 can verify resolutions against the findings rather than against a summary.

Headline: **the round-1 fixes introduced 8 new CRITICAL defects**, five of them in the seam
between C1 / C6 / C7 / C8 — which had been specified in four isolated passes and never
reconciled. Second cluster: round-1's numeric and tooling corrections were answered
narratively in §9.14 but never applied to the document body.

---

## A. Architecture re-audit — CRITICAL

| # | finding | resolution |
|---|---|---|
| 1 | Arbiter gates `ddr_dout_ready` but **not `waitrequest`**. `DDRAM_BUSY` = `ram_waitrequest` (`sys_top.v:1868`); a non-grantee seeing `!ddr_busy` issues into a mux pointed elsewhere, waits out `TIMEOUT_MAX`, loses the scanline — **every line** | §9.8: synthetic `ddr_busy = 1` to every non-grantee, both directions gated |
| 2 | Arbiter has **no timeout**; grant held "until beats return" deadlocks against the reader's `ST_WAIT_LINE` timeout escape → both masters blocked, black screen until core reload | §9.8: arbiter-side beat timeout force-releases grant + status bit; abandoning requester signals abandon |
| 3 | **ARM keepalive is a second `ctrl_word` writer** on FPGA frames. `KeepaliveTick()` writes an independent counter AND buffer bit → points the reader at the buffer being mid-filled. Tearing ~every 9th frame. M15 was closed against the wrong word | §9.11.1: **compositor never writes `ctrl_word`** — it writes a `status_word`; the ARM remains sole writer in every mode; keepalive unchanged |
| 4 | **No counter/buffer handoff across CPU_COMPOSITE transitions** in either direction → pause snapshot lands in the buffer being scanned out | §9.11.1: single owner, so no handoff exists to get wrong |
| 5 | Quiesce never says whether an **in-flight FRAME** completes and publishes; and "degrade to CPU on ack timeout" leaves a wedged compositor writing alongside the ARM | §9.11.3: publish suppression is part of quiesce; `compositor_disable` is a separate unconditional bit |
| 6 | **Script-triggered read-back cannot be flagged a frame ahead** — `getscreen()`, `blendscreen`, anigif were dropped from the C3 enumeration that claimed to be exhaustive | §9.11.5: synchronous CPU re-composite for that frame + next frame CPU_COMPOSITE; §4 count corrected 7→9 |
| 7 | **`LINEAR`'s colour key has no spare bit in RGB565** — new hole created by M4, landing on 28.8% of putsprite time | §9.7: colour key removed; 1-bit coverage plane or 32-bit scratch, bandwidth to be budgeted with M17 |
| 8 | **>30 consecutive dropped frames blanks the screen**, so "dropping is never a correctness event" was false once #3 removed the keepalive | §9.11.1: keepalive always runs → claim is now true |

## B. Architecture re-audit — MAJOR

9. §9.13's premise "the reader guards **every** wait state with `TIMEOUT_MAX`" is **false** — `ST_WAIT_AUDIO_WR` / `ST_WAIT_AUDIO_RING` have none. → §9.8 prerequisite.
10. How the five blend LUTs reach the FPGA is unspecified (runtime-built on ARM). → **still open**.
11. M10K budget understated both ways: band buffer 64/128 not 51/102 (x16 mode is 512 deep); LUTs 25 not 20; `_lutri`/`_lutbi` share a sub-table → 4 reads/cycle vs 2 ports → replicate. → §8.2 + §9.5 corrected.
12. `BLEND_HALF` is the one mode not covered by "bit-exact by construction"; `_half16` is a **masked** shift-add. → §9.5 corrected.
13. Gate action column ambiguous; every blended condition actually escalates to whole-frame. → §9.5 + §14.4.1.
14. **Downscale variant is selected PER ROW**, gated on `(uintptr_t)src_row & 15`. → §9.6 (see D2).
15. `LINEAR` source data has no defined home (background + `spriteq_add_screen` are cached-heap `s_screen`s). → **still open**.
16. 1 MB per-slot scratch unjustified; one 960×480 fallback is 921,600 B. Scratch overflow has no policy. → §9.11.4 (sizing open, overflow → CPU_COMPOSITE).
17. Nothing stops the ARM calling `video_copy_screen` → `WriteFrame` on FPGA frames. → §9.11.1 note.
18. Slot release on abandon/quiesce undefined → owner-flag leak. → §9.11.2.
19. §11 still contained the exact row M12 declared wrong. → §11 rebuilt.
20. §12's own VGA diagnostic reintroduces Option Y concern F (`band-complete` 1-cycle 98 MHz → `clk_vid`). → §11 + §12 both note it.
21. Every tooling defect from round 1 (T1–T9, N15) absent, and §14.4.5 depends on them. → **§14.5 added**.
22. `quiesce_req`/`quiesce_ack` is not a wire. → §9.11.3 + §11.
23. §6 still asserted what §9.9.3 retracts. → §6 rewritten.

Minor: 24 (§5 8-of-12 fields), 25 (census counts), 26 (§4/§15 numerics), 27 (`enable` is constant), 28 (fill gate vs FILL opcode — **still open**), 29 (M8 pointer half), 30 (beats vs bytes), 31 (legacy `ddram` is a third requester), 32 (§9.4 "~10 palettes" unmeasured — **still open**), 33 (M4 discovery timing needs a pre-scan — **still open**), 34 (E4/E2).

## C. Engine-fact verifier — discrepancies

| # | finding | resolution |
|---|---|---|
| D1 | `s_sprite` has **12** header fields, `data[]` at byte **48** on arm32, not 8/32 | §5 corrected |
| D2 | 🛑 **variant 1 may be UNREACHABLE**: `videocommon.c:127` passes `vscreen->data`; `s_screen` header 20 B; arm32 glibc malloc → base%16 ∈ {0,8} → `src_row & 15` ∈ {4,12}, never 0. All 282 320-wide PAKs would take **variant 5** | §9.6 flagged; **needs one on-device measurement** |
| D3 | LUTs are built **after** `load_models()` (`openbor.c:46510` vs `:46474`), gated on `pixelformat == PIXEL_x8` → load-time blends take the arithmetic path | §9.5 corrected |
| D4 | The doc quotes the fp path; the ship build uses inlined `putsprite_lut_` | §9.5 (partially — see round 3) |
| D5 | `usechannel` is **derived**, and both it and `tintmode` are forced to 0 when `!drawmethod->flag` | §9.5 corrected |
| D6 | **`water` does NOT displace destination content** — `gfx_draw_water` reads only the source, writing each row at a per-row x offset | §9.14 M4 corrected |
| D7 | M8's write row quotes the **8-bit** `putsprite_`, never executed by the PIXEL_16 ship build | §9.14 corrected |
| D8 | M8 omits the partial-clip **pointer** half (`count += diff; data -= diff` vs `count -= diff; data += diff`) | §9.14 corrected |
| D9 | The `frame->mask` gate rationale is wrong (masked sprites ignore the blend fn; palette selection is unaffected) | §9.3 corrected |
| D10 | Variant 1 also clamps `if (src_y >= height) src_y = height - 1` | §9.6 added |
| D11 | Two further branches exist (`bpp==8 && palette`, `bpp==32`), dead in the ship build but unmentioned | §9.6 added |

Confirmed correct: LUT sizing 5,120 B/mode, index ranges, five populated + HALF NULL, non-commutative OVERLAY/HARDLIGHT/DODGE, `blendfp(src, dest)` order, all five variant conditions verbatim, the double-truncated `(y*sy256)/256`, per-parity `rc_e`/`rc_o`, `hcnt>7` clamp, 16bpp no-255-clamp, M1 clamps present in all four box variants, v3.10 expression exact, RLE format, `PAL_BYTES = 512`.

## D. Numbers auditor

Not applied from round 1: N1 (census counts), N3 (M10K), N2 (fatal count), N9 (369 MB/s), N8 (§15 criterion), N6 (93 sprites/frame unqualified), N4 (46,001 KB unqualified), N5/M10-M11 (§6 table), N11 (19-PAK wording), N10 (sampled-fraction scoping). **All now applied.**

New errors found: LUT "~20 M10K" repeats the N3 error (25 + replication); "394 of 450" should be **393**; §9.9.3 unit convention switch (46.0 → 44.9 MiB, ~24-33 → ~24-32); per-command bound in beats vs bytes (8×); 2 px/clk insufficient — **Ninja needs 3.06 px/clk**; §9.4's "16 bands" anchored on nothing (census range 2–56). **All now applied.**

Verified exact: all 20 rows of the §8.2 band table reproduce with the clamps, **zero overlaps**; all four §9.8 arbiter timings; §14.2b internal arithmetic; §14.4.5 statistics (38.56× max, 1.05 median, 1.99 mean, 325.9/248.2/230.5/189.9/106.7 MB/s); §9.9 bench ratios; `/proc/iomem` arithmetic; `fit.summary` resource claims; `sta.summary` +0.128 ns.

## E. Consistency / completeness

Resolution-completeness at round-2 time: **2 of 8 CRITICAL and 4 of 18 MAJOR** cleanly resolved. The dominant failure mode was not bad analysis — §9.x and §9.14 mostly held — but that §4, §5, §6, §7, §8.2, §9.2, §9.7, §11, §13, §14.2, §14.4.1 and §15 were never edited to match, so the document still stated the defects its own findings described.

Contradictions found: F1 (two `ctrl_word` writers), F2 (He-Man overflows its own ring slot: 93 × 15 × 32 B = 44.6 KB vs 42.7 KB/slot), F3 (§6 vs §9.9.3), F4 (§11 CDC), F5 (Z-order unqualified), F6 (width gate exists and does not), F7 ("SEVEN paths" is never seven), F8 (working-set units), F9 (§15 criteria vs §14.4.5), F10 (four different resolution counts), F11 (reverse-stale allocator note), F12 (M10K), F13 (§14.2 vs §9.5). Stale cross-refs: F14–F18 (`9.4`/`9.5`/`12.2`/`section 14`). **All now applied.**

---

## Round-2 fixes applied (commit `c4d4c6c`)

1. **§9.11 replaced §9.11–§9.13** with one ownership model: ARM as sole `ctrl_word` writer + compositor `status_word`, a state table, abandon-releases, publish-suppression, `compositor_disable`, ring/slots/bounds, unpredicted read-back.
2. **§9.8** arbiter: gate `ddr_busy`, beat timeout, exclude the legacy `ddram` master, reader audio-state timeouts as prerequisite.
3. **44 body repairs** across §4, §5, §6, §7.1, §7.2, §8.1, §8.2, §9.2, §9.3, §9.4, §9.5, §9.6, §9.7, §9.9.3, §9.10, §9.14, §10, §11, §12, §13, §14.2, §14.2b, §14.4.1, §14.4.2, §14.4.4, §15.
4. **§14.5 added** — the measurement-tooling defects, including that 14.4.5's headroom claim is unsupported until the census is re-run.

## Still open going into round 3

- ~~D2 needs one on-device measurement~~ **CLOSED 2026-07-30.** Measured on the MiSTer
  with `tools/harness/vscreen_align_probe.c`: `offsetof(s_screen, data) = 20`, and
  20 mod 16 = 4 while glibc arm32 malloc is 8-aligned, so `data & 15` is 4 or 12 and
  **never 0** -- 0/128 screens, 0/28672 rows, on both the mmap and brk paths, and
  identically under QEMU. **Variant 1 is dead code; variant 5 carries 294 PAKs and
  moves to the FRONT of the build order** (see 9.6).
- B10 (how LUTs reach the FPGA), B15 (`LINEAR` source home for backgrounds), B28 (fill gate vs `FILL` opcode), B32 (§9.4 "~10 palettes" unmeasured), B33 (M4 discovery needs a `spriteq` pre-scan).
- Ring and per-slot scratch **sizing** (9.11.4).
- The overdraw census **re-run** with the slow path included and T5's counters widened (§14.5).
- M11's consumer enumeration for the DDR3 region.

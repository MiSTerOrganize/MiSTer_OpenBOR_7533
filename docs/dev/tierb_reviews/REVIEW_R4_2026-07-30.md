# Tier-B Phase 1 — ROUND 4 review ledger (2026-07-30)

Five reviewer runs over the post-round-3 document: a consistency pass against `3e5e616`, then
architecture / engine-fact / numbers / fix-pass-audit against `6a05840`. Fixes applied in
`6a05840` (consistency) and `33fea2b` (the rest).

**Headline: round 3's fixes contained 10 CRITICAL defects, and three round-3/4 "corrections"
were themselves wrong.** The ownership subsystem has now been rewritten FOUR times. Trend of
criticals found in the previous round's fixes: **8 → 6 → 10**. Not converging.

---

## A. Architecture — CRITICAL

| # | finding | resolution in `33fea2b` |
|---|---|---|
| C1 | 🛑 **Rule 1 publishes the buffer the compositor is about to overwrite.** `WriteFrame`'s store emits `(active_buf & 1)` and toggles AFTER; with `active_buf = completed_buf ^ 1` first, "the same store" publishes `completed_buf ^ 1` | rule 1 now uses **`KeepaliveTick`'s store**, `(!active_buf) & 1`; `WriteFrame`↔keepalive exclusion added too |
| C2 | Rule 2 gated all four sites **including `KeepaliveTick`**, which §10/§9.11.2/§14.3 require to always run — reopening defect #8 | now "the three PRESENT sites are gated; `KeepaliveTick` explicitly is not" |
| C3 | **Nothing told the compositor which framebuffer to write.** No target field anywhere; `status_word` is FPGA→ARM; no path gave it a `ctrl_word` read | ARM picks the target, passes it in `frame_flags` |
| C4 | **"At most TWO lists in flight" is arithmetically ONE** with two framebuffers; the state table granted three | corrected to ONE, with the unstated consequence recorded (**the compositor idles until the ARM publishes**) |
| C5 | Coverage plane was in the **frame header**, but four `LINEAR` kinds coexist per frame; only 16 spare bits exist in the command word | moved to the command word: mode + stride-shift in qword1 `[15:0]`, `cover_addr` reusing `pal_addr` (dead for `LINEAR`) |
| C6 | The beat-swallow is **unbounded** — it fires because beats did not arrive — and it blocks `compositor_disable` | swallow bounded; on its timeout, `fifo_aclr` reset, added to the 9.8 prerequisite list |
| C7 | "The arbiter refuses grants while it is set" is unscoped — it would starve the **video reader** (black screen) and make disable one-way | scoped to **the compositor**; takes effect at the next grant boundary |
| C8 | **ARM process restart without FPGA reset is the dominant reset path** and had no reclaim. PAK load / hot-swap / `.s1` are `exit()` + respawn, not quiesce triggers; `Init` `memset`s BOTH framebuffers while the prior compositor may be mid-burst | in-process vs exit triggers separated; **normative respawn Init sequence** added |
| C9 | The px/clk item sized the **wrong structure**: §9.2's byte-serial fetch FSM caps at ~1 px/clk regardless of banking | fetch engine recorded as the tighter bound; multi-byte run streaming + DDR3 fetch rate are co-equal constraints |
| C10 | **`mister_present` fires on every PAK load and every level load**, via `load_background()` → `video_clearscreen()` → … — not a startup path. `video_copy_screen`'s `blit()` is patched out; this one is not | promoted to a **Phase-1c/2 prerequisite**; `mister_ddr_init` must be retained (it owns the keepalive thread) |

**Architecture MAJORs (17)** — writer inventory is six `ctrl_word` writers not four; §9.8 needs a reader abandon signal + `fifo_aclr` re-arm; gate the `WriteFrame` **call** not the function (early-returning kills the golden-trace clock and audio hash); §9.5 omits the second LUT gate (`videomodes.pixel == 2`; the else branch populates 8-bit-indexed tables read through 5/6/5 macros = **wrong colours**); §5's offset is ABI-dependent; an 11th read-back path (`openbor_drawspriteq` **drains** the queue); `sdl/menu.c:474` reallocates `vscreen` at PIXEL_32; parallax `transbg` is per-layer; the doorbell has no retire semantics; `status_word`'s atomic half excludes the slot-release bitmap; the mid-frame-gate list wrongly includes the downscale variant; §14.2 omits B28; §9.11.5 does not say which frame the re-composite reproduces; "the keepalive always runs" is false before the first background load.

## B. Engine facts

| # | finding | resolution |
|---|---|---|
| D1 | 🛑 **The parallax key mechanism is wrong.** Global `pixelformat = PIXEL_x8` is never reassigned, so layer screens route to **`putscreenx8p16`**, keyed on the **palette INDEX**, not RGB565 `0x0000` via `blendscreen16`. "Black pixels included" is backwards. And `load_layer` sends `alpha>0 \|\| transbg` layers elsewhere, so a plainly-blitted screen layer has `transbg == 0` ⇒ **unkeyed** | §9.7 split into four rows by drawmethod AND source pixelformat; `transbg` recorded as per-layer/PAK-authored |
| D2 | 🛑 **The video-options menu is NOT unreachable.** `menu_options()` has two callers; `openborMain()`'s main-menu Options (`openbor.c:50735`) is **unpatched**. Software Filter is guarded only by `!fullscreen && hwscale < 2.0` — which ATOV satisfies — and setting it allocates `bscreen` **mid-session** and persists | §9.6's justification rewritten; residual widened from "imported PC config" to "settable on-device, mid-session, per PAK" |
| D3 | An 11th read-back path: `openbor_drawspriteq` composites into `vscreen` **and drains the queue** | recorded (MAJOR) |
| D4 | Writer inventory incomplete (Init/Shutdown/dummy-init); `native_audio_writer.c` is a 5th DDR3 master for M11 | added |
| D5 | Screenshot cited 4-6 lines short (gate `:45854`, call `:45856`), and predictability is **ZERO frames of lead**, not "a frame ahead" | both corrected in the canonical table AND the evidence table |
| D6 | "does not call `blend_screen16` at all" is over-broad — screen blits are unpatched | scoped to the sprite blit |
| D7 | The fetch-bound rationale is wrong: `clearcount=0, viscount=0` advances **zero** pixels, so **no pixel-derived bound exists** — that is *why* it must be a byte budget | rationale rewritten |
| D8 | R3's `putscreen` 5-arg fix was recorded but never applied | applied |
| D9 | Line drift (`screen.c` +2, `blend_half16` range, `openbor.c:50151` → `:50189`) | partially applied |

**Verified correct (stop re-litigating):** R3's A1 (`_half16` does not exist; `blend_half16` is a plain per-channel average, LUT ≡ arithmetic) and A2 (`blendscreen16` does key on `0x0000` *for a PIXEL_16 source*) — both reversals justified. `putsprite_lut_` macro-for-macro identical to the fp path. `s_savedata` decoded independently: 324 B, `swfilter` 272, `fullscreen` 288, `hwscale` 316 — **exact**. `getscreen` zero hits tree-wide. The plainsprite third palette site. All seven M8 flipx rows. Every RTL citation.

## C. Numbers

| # | error | fix |
|---|---|---|
| E8 | 🛑 **"SEVEN ran cleanly" is false — they exited 139 = SIGSEGV**, and they are exactly the diff-harness crash set (Signature A ×4, Signature B ×3), **both fixed and shipped 2026-07-23**. The gap was fully explained; the claim also contradicted the next sentence. **A round-3 fix that turned harmless vagueness into a specific falsehood** | rewritten with the crash classification and the shipped fixes |
| E9 | **"wrong for 111" is off by one** — Lust Rush at 1600 wide gets `hcnt = 5` constant from variant 5, which IS the naive `1600/320` | **340 right / 110 wrong** |
| — | 🛑 **433 MB/s has NO derivable source** (exactly 55.0% of 787.5; 591 is 75.0%); it is §15's only hard bandwidth gate and the chain is circular | added as **register item 0** |
| — | The bandwidth model **omits scanout read, palette reloads and command fetch** on the same single port | He-Man ~122.6 not 106.7; Ninja ~360; recorded |
| — | **No consolidated M10K total** anywhere | 163 (33.5%) or 203 (41.8%) of 486, before FIFOs and before M13's banking |
| — | Headless-vs-device sampling gap not in §14.5 (He-Man fast-path 96.5% headless vs 71.2% device ⇒ overdraw 2.36×, 129.0 MB/s) | **still open** |
| — | Sequence number 32 bits (§7.1) vs `completed_seq` 30 bits | **still open** |
| — | §9.6 states one of three horizontal clamps | **still open** |

**Verified exact:** all 20 band rows, zero overlaps; M10K and LUT sizing; §9.4's corrected 4.60; all five §14.4.5 rows; §14.5's T1/T5; the trace classification; arbiter timings; `status_word` fitting 32 bits; every bit-field summing to 64.

## D. Fix-pass audit (the 13 claims of `6a05840`)

2 landed-and-correct, 2 landed-but-wrong (**B1** coverage plane in the wrong structure; **B2** *there is no fade-in path* — `fade_out` occupies two sites and I relabelled them, so it is **six predictable / nine total**, not seven/ten), the rest partial. Also: **B4** the "no free slot / all F" row is unreachable under the cap; **B5** three register items are simultaneously "before Phase 2" and "Phase-2 decisions"; **B6** the §11 DDR3-poller fallback is circular; **B7** the arena-free rule needs an arbiter-grant-idle bit that `status_word` does not carry; **B10** the §3 lesson row is a malformed 1-cell row in a 3-column table and leaves the unqualified claim standing.

---

## Still open going into round 5

1. 🛑 **433 MB/s ceiling unsourced** (register item 0) — invalidates every bandwidth verdict.
2. 🛑 **Compositing rate**: fetch engine ~1 px/clk (C9) vs band buffer 2 px/clk vs 3.31 needed.
3. 🛑 **`[DCV16]`** — but the architecture reviewer found an option the doc missed: **a third, private scratch framebuffer** for the probe, no new frame mode; and §14.4.3 rungs 2+3 already establish byte-identity transitively.
4. **B7** arbiter-grant-idle bit; **B5** register vs Phase-2-decision split; **B6** the disable channel (`hps_io` is the only candidate, and it has no ARM write path).
5. **§9.5's second LUT gate** (`videomodes.pixel == 2`) and the wrong-colour 32-bit fallback.
6. **11th read-back path**; **`sdl/menu.c` reallocating `vscreen` at PIXEL_32**.
7. Doorbell retire semantics; `status_word` atomic-half packing; §9.11.5's "which frame".
8. Headless-vs-device overdraw sampling; sequence-number width; the two unstated horizontal clamps.
9. B10's malformed §3 row; §14.2 still omits B28 and E2.
10. Ring/scratch sizing, census re-run, M11 enumeration, arena write cost (carried from R3).

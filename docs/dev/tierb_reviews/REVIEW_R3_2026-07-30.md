# Tier-B Phase 1 — ROUND 3 review ledger (2026-07-30)

Four independent reviewers over the post-round-2 document (`c4d4c6c` … `cdda58d`). Rounds 1
and 2 are in `REVIEW_2026-07-30.md` and `REVIEW_R2_2026-07-30.md`. Fixes applied in `3e5e616`.

**Headline: round 2's fixes contained 6 new CRITICAL defects, and three round-2 "corrections"
were themselves wrong.** The ownership subsystem has now been rewritten three times. Round 2's
diagnosed failure mode — resolving a finding in a new section while leaving the sections that
*state* the defect untouched — recurred in ~14 places.

---

## A. Architecture — CRITICAL

| # | finding | resolution in `3e5e616` |
|---|---|---|
| C1 | 🛑 **§9.11.1's central invariant is false: `mister_present` is a THIRD `ctrl_word` writer with its OWN `mister_frame_cnt`/`mister_active_buf`** (`patch_sdl_dummy.py:41-43,154,243-245`). Four writers across TWO state sets; gating only `video_copy_screen` relocates #3/#4 rather than closing them | §9.11.1 rebuilt around the real writer inventory; all four sites gated; the two state sets must be unified before Phase 5 |
| C2 | `status_word` is subject to the torn read M5 fixes for the frame header — `completed_seq` and `completed_buf` in different 32-bit halves, and arm32 cannot atomically read 64 bits from the `O_SYNC` Device mapping → publishes the buffer being written | fields moved into ONE 32-bit word; upper half reserved for non-atomic fields |
| C3 | Nothing constrains which framebuffer the compositor writes; with 3 slots it can run 3 lists ahead of publication, so one late ARM poll lets it write into the buffer being scanned out | normative: may not target a published buffer or one holding an unpublished completion ⇒ **at most TWO lists in flight** |
| C4 | **No doorbell.** `slot[k] = A\|F` has no home, no width, no publish ordering; the memory map has no ARM→FPGA control word at all | slot-grant word added to §6 + M5 barrier-then-doorbell ordering for the whole slot |
| C5 | `compositor_disable` cannot do its job: a DDR3 word only acts when the compositor polls (a wedged one does not), and the arbiter has no DDR3 read path | promoted to an ARM-writable **register** sampled by always-alive logic; disable implies unilateral slot/scratch reclaim + a stated arena-free rule on ack timeout |
| C6 | The arbiter's beat timeout **re-creates** the corruption §9.8 exists to prevent: a late burst is late, not cancelled, and its ID-less beats are fed to the next grantee | arbiter **swallows** the abandoned burst's beats before re-granting; never revokes with beats outstanding |

## B. Architecture — MAJOR

| # | finding | status |
|---|---|---|
| MJ1 | §9.14 M12 still said "the compositor writes `ctrl_word`" | fixed |
| MJ2 | §10 contradicted itself: keepalive "never touches the sequence number" vs "must share the sequence-number state" | fixed — the shared state is `active_buf`, not the sequence number |
| MJ3 | "+1 frame by construction" no longer holds under a polled publish | now **1-2 frames, phase-dependent**, with the jitter stated |
| MJ4 | §9.11.1 mis-described the shipped keepalive; the real hazard is the `!active_buf` convention | folded into the rebuilt §9.11.1 |
| MJ5 | §9.11.2 had the ARM writing `status_word` at reset | fixed — the FPGA zeroes it |
| MJ6 | `WriteFrame -> buf^1` is not expressible and names the wrong buffer | fixed — rule 1 keeps `active_buf == published_buf ^ 1` |
| MJ7 | The new ARM↔FPGA control words were missing from the memory map | four rows added |
| MJ8 | Slot/scratch leak survives the disable path; no legal ARM action on ack timeout | folded into C5 |
| MJ9 | **2 px/clk and 3.1 px/clk are both normative**; M13's 2-bank structure caps at 2, and T1's correction puts Ninja at 3.31 | **RECORDED AS UNSETTLED**, not resolved — Phase-2 decision: 3/4-way banking (raising the M10K budget) or gate Ninja-class PAKs |
| MJ10 | The nine read-back paths disagreed across three sections; 5+3 = 8 in one place | one canonical list; screenshot is predictable |
| MJ11 | **`[DCV16]` cannot run under the ownership model** — it needs a CPU-composited `vscreen` and an FPGA framebuffer on the same frame | **RECORDED AS UNSETTLED** — §15's headline criterion depends on it; needs a dedicated `[DCV16]` frame mode |
| MJ12 | §7.2's `dst_y` was in output rows; a band is R output rows over ~3R source lines | fixed — source row, `0 .. src_lines-1` |
| MJ13 | §4 and §15 presented the bandwidth figures as measured while §14.5 declares them unsupported | both caveated |

**MINOR (fixed):** per-row fetch bound rationale (3 B/px, not 2); synthetic `ddr_busy` scoped to "while another master holds a grant" (unconditional masking deadlocks at idle); legacy `ddram` is constant-folded, so excluding it is free not required; §9.4's 4.9 MB/s; quiesce costs ~8.4 ms on a 2-band PAK; §9.12/§9.13 numbering gap; phase 1c missing the audio-timeout prerequisite.

## C. Engine facts — DISCREPANCIES

| # | finding | resolution |
|---|---|---|
| A1 | 🛑 **The `_half16` "masked shift-add" correction was WRONG** — no such macro exists; `blend_half16` is a plain per-channel `(a+b)>>1` on extracted 5/6/5 fields, and `create_half16_tbl` computes the same. **A round-2 fix that invented a defect** | reversed; HALF needs no escape hatch |
| A2 | 🛑 **"`LINEAR` cannot use a colour key" was over-broad and would BREAK byte-identity** — the engine already keys on `0x0000` (`blendscreen16`). Background = opaque memcpy (no key); parallax layers = keyed (reproducing it is required); only the rasterised fallback needs a coverage channel. **A round-2 fix that created a defect** | three-case table; coverage plane scoped to fallback scratch |
| A3 | `KeepaliveTick()` does NOT use private state — it shares `frame_counter`/`active_buf` with `WriteFrame` | premise corrected; the real hazard is the `!active_buf` convention (→ C1) |
| A4 | `openborscript.c:12148` is a player-key NAME TABLE, not a capture API. The real trigger is `openbor.c:45850-45853`, readable at the top of `update()` ⇒ **predictable** | citation fixed; screenshot moved to the predictable six |
| A5 | 🛑 **`getscreen()` does not exist in v7533** (zero hits). The real hazard is `openborvariant("vscreen")` (`openborscript.c:1132`/`:9359`) — and its WRITE side `changeopenborvariant("vscreen", …)` (`:9905`) can replace the surface outright | replaced in all four places |
| A6 | `blendscreen` is a gamelib blit, not a script path | reframed as script `putscreen`/`drawscreen` |
| A7 | **D4 still open** — `putsprite_lut_` appears nowhere; the doc quotes `blend_screen16`, which the ship build never calls for LUT modes | inlined form documented |
| A8 | A THIRD palette site: `_putsprite`'s `plainsprite` path (`drawmethod->flag == 0`) passes `sprite->palette` unconditionally, bypassing the discriminator | added |
| A9 | "the effective palette is undefined" is inaccurate — the ternary is total | corrected: deterministic; the gate exists for the both-NULL deref |
| A10 | The `src_y` clamp is shared by all five variants and **feeds the variant-1 gate**, not variant-1-scoped | corrected |
| A11 | Parallax layers (`openbor.c:45151`) pass `&screenmethod` with water/scale/rotate/repeat — not plain backgrounds | added to §9.7 |

**Citation drift (fixed):** `videocommon.c:127`→`:126` and the `engine/sdl/` prefix; `create_blend_tables_x8` at `:46513` not `:46510`; `putscreen(vscreen, fbuffer, 0, 0, &dm)`.

**D2 chain:** every source-checkable link **verified**. Two links are not source-checkable: glibc arm32 alignment (**settled by the on-device measurement**, `cdda58d`) and 🛑 **the `bscreen` branch of `getVideoSurface`** — `savedata.swfilter && (hwscale >= 2.0 || fullscreen)` returns `screen->pixels` instead of `vscreen->data`. **STILL OPEN:** the probe went around `getVideoSurface`. The settling probe is one line before the `WriteFrame` call printing `(uintptr_t)surface->data & 15, surface->pitch, surface->width, stored_videomodes.pixel`.

## D. Numbers

**Round-2's corrections: all verified correct.** Seven errors found, three of them new:

| # | error | fix |
|---|---|---|
| E1 | §9.4 `4.9 MB/s` → **4.60** (the 4.9 was the withdrawn 16-band value; round 2 renumbered the prose, not the arithmetic) | fixed |
| E2 | §8.2 `102 M10K` nine lines below the corrected 64/128 | → **128** |
| E3 | Trace coverage used PRE-correction covered-counts: `274/282` → **273/282**, `90/98` → **91/98**. Self-concealing — both versions total 19 | fixed |
| E4 | "almost all ec=1" — **12 of 19; SEVEN ran cleanly.** Round-1 N11, marked "applied" in the R2 ledger and never applied | fixed, with the seven named |
| E5 | `325.9 → ~352` scaled the background and output terms with overdraw | → **~350.3** |
| E6 | 🛑 **`wrong for 393 of 450` — introduced by the D2 commit.** With variant 1 dead, 320-wide PAKs run variant 5 where `hcnt = 1`, which is what the naive formula gives ⇒ wrong for **111**, not 393 | fixed, with the vertical-box caveat |
| E7 | Fetch bound rationale said 2 B/px; `clearcount = 0` every pixel is legal ⇒ **3 B/px** | → `src_w * 3 + 1` |

**Inconsistencies (fixed):** I1 three counts of "impossible" PAKs; I2 `34-56 fps` (superseded Phase-0 range; current anatomy is 34-48); I6 "16 times"; I3 read-back trio (→ MJ10); I5 "Measured" heading (→ MJ13); I7 3.06 from uncorrected overdraw (→ MJ9). **I4** (44.9 MB vs 46,001 KB) and **I8** (GB vs GiB column, unsorted rows) left.

**Confirmed exact:** the engine-exact census (282/98, and the `endswith("data/video.txt")` bug affects exactly one PAK); all 20 band-table rows with zero overlaps; §8.1's gcd derivation; the variant counts and all of the D2 arithmetic; LUT sizing 5,120 B / 25 KB / 25 M10K / 3 replicas; all four arbiter timings; §14.2b; §14.4.4; §14.4.5's statistics; §14.5's T1/T3/T5 figures; 431 traces; `+0.128 ns`.

## E. Consistency

Resolution completeness at round-3 time: **4 of 8 CRITICAL and 10 of 15 MAJOR** cleanly resolved; ~14 sections still stated defects their own resolutions retracted. All propagated in `3e5e616`. **Cross-references verified clean** — the §9.11 collapse and §14.5 insertion orphaned nothing.

---

## Still open going into round 4

1. ~~`bscreen` branch (D2's remaining link)~~ **CLOSED 2026-07-30.** Decoded `s_savedata`
   (324 B -- the exact struct size, so the offsets are anchored at both ends) from all
   **16** OpenBOR configs on the device: `swfilter = 0` everywhere, so the branch is dead
   and `getVideoSurface` hands over `vscreen->data`. It also cannot drift at runtime --
   the engine video-options menu lives in the stock pause menu, which
   `patches/pausemenu_patch.c` replaces with Music/SFX volume only. **Residual, now in
   9.6:** `swfilter` is PER-PAK, so an imported PC config could flip one PAK to variant 3
   -- which is exactly why the variant stays ARM-resolved per frame. Harness:
   `tools/harness/scan_savedata_swfilter.sh` + `decode_savedata.py`.
2. 🛑 **MJ9 compositing rate** — 2 px/clk specified, 3.31 needed. Phase-2 decision.
3. 🛑 **MJ11 `[DCV16]` frame mode** — the acceptance criterion's probe is impossible as written.
4. **B10** how the runtime-built blend LUTs reach FPGA M10K (no mechanism, no region, not on any open list).
5. **B15** the background/parallax `LINEAR` source has no FPGA-readable home.
6. **B28** `FILL` opcode vs "fill → CPU" gate — one is dead.
7. **B32** §9.4's "~10 distinct palettes per band" is unmeasured and is the sole input to its bandwidth.
8. **B33** whole-frame gates (water/tint/unsupported mode) are discovered mid-`spriteq_draw`, after the mode is committed — needs a pre-scan or a retroactive abandon.
9. Ring and per-slot scratch **sizing**.
10. The overdraw census **re-run** with the slow path included and T5's counters widened.
11. M11's DDR3 consumer enumeration.
12. §14.2 is not a real open-items register — it omits most of the above.
